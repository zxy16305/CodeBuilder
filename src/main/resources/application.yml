spring:
  datasource:
    url: jdbc:mysql:///DB_TEST?useSSL=false
    username: root
    password: root
  mvc:
    static-path-pattern: /static/**
  resources:
    static-locations: classpath:/static/


